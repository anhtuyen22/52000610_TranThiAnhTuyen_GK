const routes = {
  login: '/login',
  register: '/register',
  products: '/',
  detail: '/detail/:productId',
  cart: '/cart',
  checkout: '/checkout',
  checkoutDone: '/checkout-done',
  userPurchase: '/user/purchase',
};

export default routes;
